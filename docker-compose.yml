services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  mongodb:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  webhook-server:
    build:
      context: .
      dockerfile: Dockerfile.webhook-server
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MONGODB_URI=mongodb://mongodb:27017/github-sentinel
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET:-dev-secret}
    depends_on:
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy

  event-worker:
    build:
      context: .
      dockerfile: Dockerfile.event-worker
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MONGODB_URI=mongodb://mongodb:27017/github-sentinel
      - SUSPICIOUS_PUSH_TIMEZONE=${SUSPICIOUS_PUSH_TIMEZONE:-UTC}
      - SUSPICIOUS_PUSH_START_HOUR=${SUSPICIOUS_PUSH_START_HOUR:-14}
      - SUSPICIOUS_PUSH_END_HOUR=${SUSPICIOUS_PUSH_END_HOUR:-16}
      - RAPID_DELETE_WINDOW_MINUTES=${RAPID_DELETE_WINDOW_MINUTES:-10}
    depends_on:
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    deploy:
      replicas: 2

volumes:
  mongodb_data:
